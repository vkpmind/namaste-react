let heading = React.createElement(
  "h1",
  { id: "react-id", className: "react-class" },
  "I am from React JS..."
);
let root = ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);
